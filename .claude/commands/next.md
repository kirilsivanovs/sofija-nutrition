---
description: Continue — same as /work with no argument. Does the next step on whatever task is most ready.
---

Do exactly what `.claude/commands/work.md` specifies, with `$ARGUMENTS` as its argument. Read that file and follow it — it is the single definition of how a step is routed.
