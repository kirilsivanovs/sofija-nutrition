---
/**
 * Universal Application Header Component
 * Unified header design for admin, cabinet, and booking modules
 *
 * Props:
 * - variant: 'admin' | 'cabinet' | 'public'
 * - showBurger: boolean (show burger menu for mobile)
 * - showAuth: boolean (show login/logout)
 * - subtitle: string (header subtitle text)
 */

interface Props {
  variant?: 'admin' | 'cabinet' | 'public';
  showBurger?: boolean;
  showAuth?: boolean;
  subtitle?: string;
}

const { variant = 'public', showBurger = false, showAuth = true, subtitle = '' } = Astro.props;

// Generate unique IDs for mobile menu
const mobileMenuId = `mobile-nav-menu-${variant}`;
const logoutUrl = '/.auth/logout?post_logout_redirect_uri=/';
---

<header class={`app-header app-header--${variant}`}>
  <div class="app-header-inner">
    {
      showBurger && (
        <button
          type="button"
          class="mobile-menu-btn"
          style="-webkit-tap-highlight-color:transparent;touch-action:manipulation;"
          aria-label="Atvērt izvēlni"
          aria-expanded="false"
          aria-controls={mobileMenuId}
        >
          <span class="w-6 h-0.5 bg-primary transition-all" style="pointer-events:none;" />
          <span class="w-6 h-0.5 bg-primary transition-all" style="pointer-events:none;" />
          <span class="w-6 h-0.5 bg-primary transition-all" style="pointer-events:none;" />
        </button>
      )
    }

    <a href="/" class="app-logo">
      <img src="/assets/img/logo.svg" alt="Sofija Ivanova" class="app-logo-img" />
      <div class="app-logo-text">
        <span class="app-logo-name">Sofija Ivanova</span>
        {subtitle && <span class="app-logo-sub">{subtitle}</span>}
      </div>
    </a>

    {
      showAuth && (
        <div class="app-auth">
          {variant === 'admin' ? (
            <>
              <span id="user-info" class="app-user-name" />
              <a href={logoutUrl} class="app-logout">
                <i class="ph ph-sign-out" />
                <span>Iziet</span>
              </a>
            </>
          ) : (
            <>
              <div id="auth-actions" class="app-auth-actions">
                <a
                  class="btn-login"
                  id="login-aad"
                  href={`/.auth/login/aad?post_login_redirect_uri=${variant === 'cabinet' ? '/cabinet' : '/'}`}
                >
                  <i class="ph ph-sign-in" />
                  <span class="login-text">Pieslēgties</span>
                </a>
              </div>
              <div id="auth-user" class="app-auth-user hidden">
                <span id="user-name" class="app-user-name" />
                <a id="logout-link" href={logoutUrl} class="app-logout">
                  <i class="ph ph-sign-out" />
                </a>
              </div>
            </>
          )}
        </div>
      )
    }
  </div>
</header>

{
  showBurger && (
    <div id={mobileMenuId} class="mobile-nav-menu">
      <slot name="mobile-menu" />
    </div>
  )
}

<style>
  /* Header Base Styles */
  .app-header {
    background: var(--color-white, #ffffff);
    border-bottom: 2px solid var(--color-primary, #1b4332);
    padding: 14px 20px;
    position: sticky;
    top: 0;
    z-index: 1000;
    box-shadow: 0 2px 8px rgba(27, 67, 50, 0.08);
  }

  .app-header-inner {
    max-width: 1400px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 20px;
  }

  /* Logo Styles */
  .app-logo {
    display: flex;
    align-items: center;
    gap: 14px;
    text-decoration: none;
    transition: opacity 0.2s;
  }

  .app-logo:hover {
    opacity: 0.85;
  }

  .app-logo-img {
    height: 42px;
    width: auto;
  }

  .app-logo-text {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .app-logo-name {
    font-family: var(--font-serif, 'Playfair Display', serif);
    font-size: 23px;
    font-weight: 700;
    color: var(--color-primary, #1b4332);
    line-height: 1.1;
  }

  .app-logo-sub {
    font-size: 11px;
    color: var(--color-text-light, #6b7280);
    text-transform: uppercase;
    letter-spacing: 2px;
    font-weight: 500;
  }

  /* Auth Styles */
  .app-auth {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .app-user-name {
    font-size: 14px;
    color: var(--color-text, #1f2937);
    font-weight: 500;
    display: none;
  }

  .app-logout,
  .btn-login {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 14px;
    color: var(--color-text-light, #6b7280);
    text-decoration: none;
    padding: 10px 18px;
    border-radius: 8px;
    transition: all 0.2s;
    border: 1px solid transparent;
    cursor: pointer;
  }

  .app-logout:hover {
    background: #fef2f2;
    color: #dc2626;
    border-color: #fecaca;
  }

  .btn-login {
    background: var(--color-primary, #1b4332);
    color: white;
    border-color: var(--color-primary, #1b4332);
  }

  .btn-login:hover {
    background: var(--color-accent, #295d45);
    border-color: var(--color-accent, #295d45);
  }

  /* Mobile Menu Button */
  .mobile-menu-btn {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 6px;
    cursor: pointer;
    z-index: 999;
    position: relative;
    background: none;
    border: none;
    padding: 12px;
    min-width: 48px;
    min-height: 48px;
    -webkit-tap-highlight-color: transparent;
    -webkit-touch-callout: none;
    -webkit-user-select: none;
    user-select: none;
    touch-action: manipulation;
  }

  .mobile-menu-btn span {
    display: block;
    width: 24px;
    height: 2px;
    background: var(--color-primary, #1b4332);
    border-radius: 2px;
    transition: all 0.3s ease;
    pointer-events: none;
  }

  .mobile-menu-btn:hover span {
    background: var(--color-accent, #295d45);
  }

  /* Hamburger animation to X */
  .mobile-menu-btn.active span:nth-child(1) {
    transform: rotate(45deg) translate(5px, 6px);
    background: var(--color-primary, #1b4332);
  }

  .mobile-menu-btn.active span:nth-child(2) {
    opacity: 0;
  }

  .mobile-menu-btn.active span:nth-child(3) {
    transform: rotate(-45deg) translate(5px, -6px);
    background: var(--color-primary, #1b4332);
  }

  /* ── Premium Fullscreen Mobile Menu ── */
  .mobile-nav-menu {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    padding-top: 72px; /* header height */
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 0;
    padding-left: 40px;
    padding-right: 40px;
    padding-bottom: 60px;
    background: linear-gradient(170deg, #ffffff 0%, var(--color-cream, #faf6ef) 50%, #f5efe5 100%);
    z-index: 999;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transition:
      opacity 0.4s cubic-bezier(0.4, 0, 0.2, 1),
      visibility 0.4s;
    overflow-y: auto;
    -webkit-overflow-scrolling: touch;
    overscroll-behavior: contain;
  }

  /* Decorative gold accent line */
  .mobile-nav-menu::before {
    content: '';
    position: absolute;
    top: 50%;
    right: 40px;
    width: 1px;
    height: 0;
    background: linear-gradient(180deg, transparent, var(--color-secondary, #c9a36a), transparent);
    transform: translateY(-50%);
    transition: height 0.6s 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    opacity: 0.5;
  }

  .mobile-nav-menu.open::before {
    height: 60%;
  }

  .mobile-nav-menu > * {
    position: relative;
    z-index: 1;
  }

  .mobile-nav-menu.open {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
  }

  /* Premium nav items */
  .mobile-nav-menu :global(a),
  .mobile-nav-menu :global(button) {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 16px 24px;
    border-radius: 12px;
    font-family: var(--font-serif, 'Cormorant Garamond', serif);
    font-size: 1.35rem;
    font-weight: 500;
    color: var(--color-primary, #1b4332);
    text-decoration: none;
    background: transparent;
    border: none;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    min-height: 56px;
    width: 100%;
    max-width: 320px;
    box-shadow: none;
    position: relative;
    letter-spacing: 0.02em;
    /* Staggered entrance */
    opacity: 0;
    transform: translateX(-20px);
  }

  .mobile-nav-menu.open :global(a),
  .mobile-nav-menu.open :global(button) {
    opacity: 1;
    transform: translateX(0);
  }

  .mobile-nav-menu.open :global(:nth-child(1)) {
    transition: all 0.4s 0.08s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .mobile-nav-menu.open :global(:nth-child(2)) {
    transition: all 0.4s 0.14s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .mobile-nav-menu.open :global(:nth-child(3)) {
    transition: all 0.4s 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .mobile-nav-menu.open :global(:nth-child(4)) {
    transition: all 0.4s 0.26s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .mobile-nav-menu.open :global(:nth-child(5)) {
    transition: all 0.4s 0.32s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .mobile-nav-menu.open :global(:nth-child(6)) {
    transition: all 0.4s 0.38s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .mobile-nav-menu :global(a:hover),
  .mobile-nav-menu :global(button:hover) {
    color: var(--color-primary, #1b4332);
    background: rgba(27, 67, 50, 0.05);
    transform: translateX(4px);
    box-shadow: none;
  }

  .mobile-nav-menu :global(.mobile-nav-item) {
    justify-content: flex-start;
  }

  .mobile-nav-menu :global(.mobile-nav-item.active) {
    color: var(--color-primary, #1b4332);
    background: rgba(27, 67, 50, 0.06);
    font-weight: 600;
  }

  .mobile-nav-menu :global(.mobile-nav-item.active)::before {
    content: '';
    position: absolute;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 3px;
    height: 24px;
    background: var(--color-secondary, #c9a36a);
    border-radius: 0 3px 3px 0;
  }

  /* Responsive */
  @media (max-width: 480px) {
    .app-header {
      padding: 12px 16px;
    }
    .app-logo-img {
      height: 38px;
    }
    .app-logo-name {
      font-size: 18px;
    }
    .app-logo-sub {
      font-size: 9px;
      letter-spacing: 1.5px;
    }
    .app-logout span,
    .login-text {
      display: none;
    }
    .app-logout,
    .btn-login {
      padding: 8px;
      font-size: 20px;
    }
  }

  @media (min-width: 600px) {
    .app-user-name {
      display: block;
    }
  }

  @media (min-width: 768px) {
    .mobile-menu-btn,
    .mobile-nav-menu,
    .mobile-nav-menu.open {
      display: none;
    }
    .app-header {
      padding: 18px 32px;
    }
    .app-logo-img {
      height: 48px;
    }
    .app-logo-name {
      font-size: 26px;
    }
    .app-logout span,
    .login-text {
      display: inline;
    }
  }

  /* Variant-specific adjustments */
  .app-header--cabinet {
    border-bottom: 1px solid #e5e7eb;
  }

  .app-header--public {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(8px);
  }
</style>

<script is:inline>
  // Mobile menu functionality - inline for iOS Safari compatibility
  (function () {
    function initBurgerMenu() {
      const btns = document.querySelectorAll('.mobile-menu-btn');

      btns.forEach(function (btn) {
        if (btn.dataset.burgerBound === 'true') return;
        const menuId = btn.getAttribute('aria-controls');
        const menu = document.getElementById(menuId);
        if (!menu) return;
        btn.dataset.burgerBound = 'true';

        function toggle(e) {
          e.preventDefault();
          e.stopPropagation();
          const isOpen = menu.classList.toggle('open');
          btn.classList.toggle('active', isOpen);
          btn.setAttribute('aria-expanded', String(isOpen));
          document.body.style.overflow = isOpen ? 'hidden' : '';
          document.body.style.position = isOpen ? 'fixed' : '';
          document.body.style.width = isOpen ? '100%' : '';
          document.body.style.top = isOpen ? '0' : '';
        }

        function close() {
          menu.classList.remove('open');
          btn.classList.remove('active');
          btn.setAttribute('aria-expanded', 'false');
          document.body.style.overflow = '';
          document.body.style.position = '';
          document.body.style.width = '';
          document.body.style.top = '';
        }

        // Add click handler to button
        btn.addEventListener('click', toggle, false);
        btn.addEventListener(
          'touchstart',
          function (e) {
            e.preventDefault();
            toggle(e);
          },
          { passive: false }
        );

        // Close on link/button click inside menu
        const links = menu.querySelectorAll('a, button[data-tab]');
        for (let i = 0; i < links.length; i++) {
          links[i].addEventListener('click', close, false);
        }

        // Close on overlay click
        menu.addEventListener(
          'click',
          function (e) {
            if (e.target === menu) close();
          },
          false
        );
      });
    }

    // Initialize immediately if DOM is ready
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initBurgerMenu);
    } else {
      initBurgerMenu();
    }
  })();

  // Logout: Azure SWA handles session cleanup server-side via /.auth/logout
  // No need to manually clear cookies/storage — auth cookies are httpOnly.
  // The <a href="/.auth/logout?post_logout_redirect_uri=..."> link works as-is.
</script>

<script>
  // Initialize auth state for cabinet variant
  if (document.querySelector('.app-header--cabinet')) {
    const authActions = document.getElementById('auth-actions');
    const authUser = document.getElementById('auth-user');
    const userName = document.getElementById('user-name');

    // Check authentication (this will be called by cabinet.astro's auth logic)
    (window as any).updateHeaderAuth = (user: any) => {
      if (user && authActions && authUser && userName) {
        authActions.classList.add('hidden');
        authUser.classList.remove('hidden');
        userName.textContent = user.userDetails || user.userId || '';
      }
    };
  }
</script>
